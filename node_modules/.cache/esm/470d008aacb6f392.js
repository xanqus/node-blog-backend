let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_acb‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_acb‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_acb‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_acb‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_acb‍.w("./api",[["default",["api"],function(v){api=v}]]);_acb‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
  })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _acb‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(4000, () => {
  _acb‍.g.console.log('Listening to port %d', port);
});
