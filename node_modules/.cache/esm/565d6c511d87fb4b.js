let jwt;_1d8‍.x([["default",()=>_1d8‍.o]]);_1d8‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
  const token = ctx.cookies.get('access_token');
  if (!token) {
    console.log('no token!!!');
    return next();
  }
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    ctx.state.user = {
      _id: decoded._id,
      username: decoded.username,
    };
    _1d8‍.g.console.log('state', decoded._id, decoded.username);
    _1d8‍.g.console.log(decoded);
    console.log('token!!!');
    return next;
  } catch (e) {
    return next();
  }
};

_1d8‍.d(jwtMiddleware);
